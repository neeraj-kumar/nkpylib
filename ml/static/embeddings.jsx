// Note that this is imported in the main HTML file directly, so you have to use React.xxx for everything

const Main = () => {
  return <div>Embeddings Page</div>;
};

ReactDOM.createRoot(document.getElementById("main")).render(<Main />);
